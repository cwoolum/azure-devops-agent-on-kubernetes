agent:
  organizationUrl: "https://dev.azure.com/buraktungut"
  pat: "dummy-pat-token-for-testing"

volumes:
  - name: dockersock
    hostPath:
      path: /var/run/docker.sock
volumeMounts:
  - name: dockersock
    mountPath: "/var/run/docker.sock"